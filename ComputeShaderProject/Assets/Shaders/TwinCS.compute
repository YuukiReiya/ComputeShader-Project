// Each #kernel tells which function to compile; you can have many kernels
#pragma kernel Sample
RWStructuredBuffer<int>buffer;
[numthreads(4,4,1)]
void Sample (int3 id : SV_DispatchThreadID)
{
	int ret = id.x + id.y * 8;
	buffer[ret] = ret;
}
